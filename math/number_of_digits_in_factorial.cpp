 /**
    Formulas :

    Number of digits of a number n = log(n)+1;
    Number of digits of  n! = log(1*2*3*4*.......*n)+1;
                            =log(1)+log(2)+log(3)+........log(n)+1;
    So, in base b logb(n!)=log(n!)*logb(10)+1;
                          =(log(n!)/log(b))+1

    log is 10 base.....
    */
